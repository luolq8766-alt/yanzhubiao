# 研助表 2.0 升级指南（已有线上系统）

本次需要更新 **Supabase 数据库 + GitHub 网页源码**。只更新其中一个不能完整启用新功能。现有账号、邀请名单和账目继续使用，不需要新建 Supabase 项目或 GitHub 仓库。

## 第一步：同步并备份

1. 请所有成员把旧网页中的待同步记录同步完成，升级期间暂缓记账。
2. 老师在旧版“设置 → 导出本机数据备份”保存 JSON；另导出全年 Excel。JSON 只包含当前账号有权读取的数据，不等于完整数据库备份，涉及云数据库恢复时应另做数据库备份。
3. 本机旧源码已另存为 `F:\矿大\课程作业\研助表-升级前源码备份-20260912.zip`。它是代码备份，不含线上财务数据。

## 第二步：在 Supabase 执行数据库升级

1. 打开你正在使用的 Supabase 项目，进入 **SQL Editor → New query**。
2. 用记事本打开更新包中的 `supabase/migrations/202609120001_v2.sql`。
3. 复制文件**全部内容**到 SQL Editor，点击 **Run**。
4. 执行成功后继续下一步。该脚本在一个事务内升级，可重复执行；保留已有记录及原日期、金额，增加新字段和功能。

**已有系统只运行这个升级 SQL，不要重新运行 `schema.sql`，不要删除表，也不用再次执行邀请示例。** `schema.sql` 是为全新空数据库准备的。把 SQL 文件上传 GitHub 只会保存代码，不会自动修改 Supabase。

你已有的 `yanzhu-monthly-allowance` Cron 不必重新创建。任务仍调用原函数名，但升级后函数只按新工资标准生成“固定工资”；每月浮动补助改为老师明确填写。没有配置新工资标准之前，不会凭空新增工资。

## 第三步：把新源码放到 GitHub 的 main 分支

仓库：[luolq8766-alt/yanzhubiao](https://github.com/luolq8766-alt/yanzhubiao)

推荐使用 `研助表-v2.0-GitHub更新包.zip`：先解压，将解压后根目录里的文件/文件夹上传到仓库根目录，保留文件夹层级并覆盖同路径文件。不要把 ZIP 本身上传，也不要多套一层“更新包”目录。

必须更新的文件结构：

```text
仓库根目录
├─ package.json
├─ pnpm-lock.yaml
├─ pnpm-workspace.yaml
├─ README.md
├─ 升级指南-v2.0.md
├─ 验收说明.md
├─ src/
│  ├─ App.jsx
│  ├─ Payroll.jsx              新增：工资与月度补助管理
│  ├─ domain.js
│  ├─ store.js
│  ├─ exports.js
│  ├─ main.jsx
│  └─ styles.css
├─ supabase/
│  ├─ schema.sql               仅新项目初始化
│  └─ migrations/
│     └─ 202609120001_v2.sql    本次数据库升级脚本
├─ tests/                     全部覆盖，含 fixtures/schema-v1.sql
├─ scripts/
└─ .github/workflows/deploy.yml
```

说明：

- 你截图中的“来源”“苏巴斯”“脚本”“工作流”是浏览器翻译，实际文件夹名称是 `src`、`supabase`、`scripts`、`workflows`，不要真的把目录重命名为中文。
- 你仓库根目录还放了重复的 `App.jsx`、`domain.js`、`schema.sql` 等。网页加载的是 `src/main.jsx`，因此必须更新 **src 文件夹里的源码**。只改根目录的重复文件不会更新网页。那些重复文件可暂时保留，它们不参与构建。
- 保留原来的 `public` 图标和字体文件夹。精简更新包不重复携带未改动的字体；“完整源码包”包含它们。
- 不上传 `node_modules`、真实 `.env`、个人备份、财务导出或邀请名单。无需手动上传 `dist`，Actions 会从源码生成它。
- `.github` 已有正确工作流，本次覆盖同路径文件即可；不要新建第二个同名部署流程。若浏览器遗漏隐藏文件，使用 GitHub Desktop 同步更稳妥。

提交后进入 **Actions → Deploy Yanzhubiao**，等待新的一次运行变成绿色。若没有自动运行，点击 **Run workflow → main**。

GitHub Variables 继续使用原有两个值，无需换 key：

```text
VITE_SUPABASE_URL = https://viydrzxnbvgkadcwqvki.supabase.co
VITE_SUPABASE_ANON_KEY = 你已有的公开 publishable / anon key
```

项目 URL 不加 `/rest/v1/`、`/auth/v1/`，也不是 Supabase 控制台的网址。不要把管理员 secret、service_role 或数据库密码填入前端。

## 第四步：刷新并核对新功能

打开原网址：[研助表](https://luolq8766-alt.github.io/yanzhubiao/)。等待应用更新后刷新，再进入“设置”，底部应显示 **版本 2.0.0**。

手机桌面版可能暂时保留旧页面。先关闭再重新打开；仍是旧版时用浏览器打开同一链接刷新，或在无痕窗口检查新版本。**有离线待同步数据时，不要清除网站数据或卸载 PWA。** 先完成同步或导出本机备份。

### 1. 指定月份的补助，以及 2000 改 4000

“设置 → 按月发放与更正”：选择学生、所属年月、月度补助；输入该月总额保存。原来 2000 改为 4000，就填写 4000，不是再加一笔 4000。数据库保留修改审计，相同成员相同月份只有一条这种月度补助记录。

老师也可在财务流水中编辑旧版锁定的补助金额。月度记录不允许直接改年月，以免把两个不同月份的钱混在一起。若旧账记错月：先核对两个月已有款项，将错月金额更正为实际应有金额，再在正确月份填写。

**关于王光浩 8 月的 2000 元：** 截图显示 8 月只有一笔支出，但年度差额含 2000 元收入。这表示需在“全年汇总”中查找那条旧补助的实际日期；升级不会猜测并挪动历史日期。如果确实记到 9 月而实际属于 8 月，请按上一段更正，并同时核对 9 月是否应有独立补助。

如果该月还存在通过“记一笔”单独录入的额外补助，设置页会提示其笔数和金额。它们仍参与总计，请避免重复录入同一笔款项。

### 2. 固定工资

“设置 → 按年级设置固定工资”：选择标准生效年月，分别设置硕一至硕三、博一至博五金额。例如硕二填 1000。到每月自动记账时生成“固定工资”，学生收入增加，老师团队支出也增加。

- 现有档案是自然年度制：按 `登记年级 + 报表年 − 首份报表年份` 确定年级，即每年 1 月变化；不是自动按 9 月学年制晋级。
- 生效月以前的工资不变；生效月起、下一条工资标准生效之前的自动工资会按新标准重算。
- 在“按月发放与更正”中选择“固定工资”，可单独更正某学生某月金额；这种更正不会再被年级标准覆盖。
- 自动工资从成员记账起始月份生成；起始月份以前的在读年月可手动补录。未来月份不会提前自动生成，可由老师按月明确登记。
- 旧版“研助补助”原样保留为补助，不自动变成工资。因此新工资标准生效之前，请核对旧款项的真实含义，避免工资与补助重复。

### 3. 日期范围、多项费用与出差工资

学生“记一笔 → 科研支出 / 出差事务”可填开始、结束日期，并添加机票、住宿、餐饮、打车等明细。每项有独立名称、分类、金额；支持 0 元，分类增加“招待费”。

勾选“这是出差事务”，填写下井天数，系统计算：

```text
出差天数 = 结束日期 − 开始日期 + 1
出差工资 = 出差天数 × 120 元 + 下井天数 × 60 元
2026-07-19 至 2026-07-30 = 12 天
下井 3 天：12 × 120 + 3 × 60 = 1620 元
费用：2000 + 200 + 200 + 200 = 2600 元
```

费用和工资分开统计；关联工资由原事务派生，修改或删除原事务时同步更新，不是一笔可任意修改的独立收入。服务端重新计算工资，不信任浏览器传入的工资总额。下井天数必须为整数，不能大于出差天数。

**跨月口径：** 当前整笔费用和关联工资都计入开始日期所在月，不按天拆分到多个报表月。一笔事务共用同一种支付方式；若一部分个人垫付、一部分课题组支付，请分笔登记。结束日期也必须在成员在读年度内。

### 4. 奖金与任务

学生可在“记一笔 → 奖金（自行录入）”中登记奖金。老师发布任务时填写单独的数字字段“悬赏奖金”；验收任务后，奖金自动记入领取学生账单。重复验收不重复记账，其他学生仍只能看到任务已被选择。

旧任务的现金金额默认 0，不自动解析奖励说明里的数字。老师可先点“编辑任务 / 奖金”填写正确金额，再验收。已经完成的旧任务不会补发；已手工发放过奖金的任务请保持现金金额 0，避免重复。

### 5. Excel

页面顶部“导出 Excel”直接下载标准 `.xlsx`，包含“财务明细”“成员汇总”“费用分项”三个工作表，金额为 Excel 数值单元格，支持筛选和后续编辑。先选年月、成员，导出范围与报表筛选一致；搜索关键字仅筛选屏幕显示，不缩小导出范围。

另有文件图标保留 PDF 导出。设置中的 JSON 是备份，不是财务报表。微信内置浏览器可能拦截文件下载，请用 Safari / Chrome 打开原网址下载，再用 Excel / WPS 打开。

## 财务口径

```text
学生收入 = 固定工资 + 月度/额外补助 + 出差工资 + 奖金
科研费用 = 个人垫付 + 个人承担 + 课题组直接支付的科研费用
月 / 年 / 总差额 = 学生收入 − 科研费用
待报销 = 个人垫付 − 已确认报销
个人实际净收入 = 学生收入 − 待报销 − 个人承担
团队已记账支出 = 学生收入 + 课题组直接支付 + 已确认报销
团队费用（含待报销）= 团队已记账支出 + 待报销
```

团队费用不包含学生个人承担的部分。工资、补助、奖金均为账目记录，不执行银行付款；“已记账支出”也不证明款项实际到账。按支出所属月份展示的已报销金额是截至当前的状态，并非该月实际发生的银行现金流。

## 最后核对

1. 老师给学生 A 的某月补助填 2000，再改 4000；A 和老师同月总览都显示 4000，该月无重复补助。
2. 配置硕二月工资 1000，确认适用月份账单出现固定工资。把其中一个月单独改为 1200，再改年级标准，确认这个单独更正月保持 1200。
3. A 录入示例出差，费用 2600、工资 1620；老师看到同一结果，学生 B 看不到 A 的账目。
4. A 离线录入一笔，重开仍保留；联网后等待同步完成，老师可见。
5. A 领取任务，老师验收后新增一次奖金；B 只能看到已占用/已完成。
6. 导出 Excel，核对三个工作表与页面金额一致。

本次交付已完成本机数据库模拟、升级保留检查、计算及浏览器交互验证；没有替你执行生产数据库 SQL 或提交 GitHub。真实账号跨设备、邮件及云端 Cron 需在你完成以上部署后核对。
