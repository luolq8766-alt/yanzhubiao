# 研助表 2.0 验收记录

## 自动检查

- Node 测试：培养年限、整数分、日期范围、月年总统计、费用分类、零元、出差工资、派生工资不重复、离线持久队列、失去服务器确认后重试、版本冲突、多窗口写入、超过 1000 条的分页读取、PWA 仓库子路径离线。
- PGlite 执行真实 PostgreSQL 业务 SQL，Auth 身份为测试桩：学生隔离、老师确认报销、任务领取隐私、一次领取、月度补助 2000→4000、工资标准重算与手动更正保护、服务端出差工资计算、自填奖金、任务验收自动奖金且重试不重复。
- 独立升级测试：在 1.0 数据库插入已有成员和 2000 元补助，连续执行两次升级；逐字段检查历史记录不变，邀请名单和账号保留，不额外生成工资；旧补助可原 ID 修改到 4000 元，存在审计。
- Excel：生成并重新解析 `.xlsx`，检查三个工作表、金额为数值类型。PDF：嵌入完整中文字体、自动多页。导出文件是演示数据，不含生产数据。
- 本机 Chrome 无头浏览器：桌面和 390px 手机尺寸交互，补助修改与总览、原生 xlsx 下载、多项出差、工资联动、零元招待费、学生奖金、任务领取隐私和验收奖金。浏览器测试使用独立临时用户目录，不读取用户登录账号。
- 生产构建通过，生成带内容指纹的 Service Worker。未配置云环境变量的本机版本是演示版。

## 仍需上线核对

未替用户提交 GitHub 或执行生产 Supabase SQL，也没有访问真实财务数据。按升级指南部署后，以老师、学生 A、学生 B 实测：年月补助一致、A 的账目 B 不可见、任务隐私、断网恢复后同步、真实 Cron 日志、Excel 与账目一致。

## 明确边界

- 培养报表与年级递进使用自然年度制；跨月出差归开始日期的月份。
- 一笔事务可多项费用，但共用支付方式；不同支付方式分笔。
- 旧补助不会自动更改日期或变成工资；旧任务现金奖励默认 0，已完成任务不追溯补发。
- 工资和补助只是记账，不做银行付款。学生自填奖金并不等于老师已经核验的银行到账。
- 月度管理和任务操作必须联网；普通支出、自填奖金可以离线。关闭 iOS 应用不保证后台同步。
- PDF 通用排版和 Excel 可编辑报表需按学校实际模板确认，不等同学校已审核凭证。
- 本机 JSON 备份无自动导入覆盖功能，完整云数据应另做数据库备份。
