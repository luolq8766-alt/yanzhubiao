# 研助表 v1.0 交付说明

交付日期：2026-09-12。

本包包含前端源码、数据库初始化及权限脚本、每月定额任务、桌面图标、中文字体、GitHub Pages 自动部署流程和中文部署教程。配置数据库后面向 1 名老师、9 名学生使用。

## 已完成验证

- 13 项自动化检查通过：培养年限、整数金额、净收入公式、预算警戒、日期校验、离线持久队列、重开恢复、网络失败、版本冲突、多页面写入、千条以上记录分页、GitHub 子路径离线首页回退。
- 21 项 PostgreSQL 检查通过：老师角色授权、个人账目隔离、学生禁止修改报销/收入、禁止提权、请求幂等、版本校验、任务领取与身份隐私、定额流水去重及金额变更规则。
- 实际生成 Excel 和含中文字体的 PDF，检查 Excel 数值单元格和汇总表，渲染 PDF 检查中文与分页。
- 浏览器交互验证：新增账目、搜索、导出、老师/学生切换、领取任务、老师查看领取者、硕二只显示两年。
- 桌面及 390px 手机视口检查，无页面整体横向溢出；宽财务表可以在表格内部横向滑动。
- 生产构建成功。

## 尚需在你的账号中完成

1. 建立 Supabase 项目并执行 SQL，添加真实邀请邮箱。
2. 创建 GitHub 仓库，上传解压后的源码，配置两个公开构建变量并开启 Pages。
3. 配置邮件验证和网页回跳链接。
4. 在真实手机与学校网络上完成老师、学生 A、学生 B 三账号联调，并查看 Cron 运行记录。

**当前没有创建真实 GitHub 仓库、正式互联网链接或真实云数据库。** 未配置云数据库时只运行带明确提示的独立演示版。

数据库测试采用 PGlite 和模拟 Auth 身份；同步测试采用可控网络替身，不等于真实 Supabase 服务已经上线。iPhone 与安卓检查为响应式视口和 PWA 逻辑验证，未连接实体手机。

自动补助功能是定额计算与流水生成，不执行银行转账。iOS 关闭应用后不保证后台同步；恢复网络并打开应用会重试。当前报表是通用格式，提交学校前应核对本校所需签字和模板。
